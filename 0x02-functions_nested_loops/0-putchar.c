#include "0-putchar.h"
#include <stdio.h>
#include <string.h>
/**
 * main - calls _putchar() from header file
 *
 * Return: 0 (success)
 */

int main(void)
{
//	for (i = 0; i <= strlen(c); i++)
		_putchar("_putchar");

	_putchar("\n");
	//printf("%lu", strlen(c));
	return (0);
}
