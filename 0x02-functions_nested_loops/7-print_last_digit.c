#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - Print last digit of a number
 *
 * @n: character to assess
 *
 * Return: (0);
 */

int print_last_digits(int n)
{
		printf((n % 10) + '0');
		return (0);
}
