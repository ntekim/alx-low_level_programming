#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: pointer variable of type char to be worked with
 */

int _strlen(char *s)
{
	int len;
	len = strlen(*s);
	return (len);
}
