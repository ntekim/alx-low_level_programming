#include "main.h"
/**
 *  _puts - prints a string followed by a new line
 *  @str : pointer variable of the string to be printed out
 */

void _puts(char *str)
{
	while (*str)
		_putchar(*s++);

	_putchar('\n');
}
